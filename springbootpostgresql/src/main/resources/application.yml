spring:
  datasource:
    druid:
      url: jdbc:postgresql://localhost:5432/postgres
      username: postgres
      password: 111111
      driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: "update"
