<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yaoge.dao.secondary.ManMapper">
    <!--int insertUser();-->

    <select id="selectPeoples" resultType="man">
        select * from man
    </select>
    <insert id="insertPeople" >
        insert into man values(null,'dss',23)
    </insert>
</mapper>