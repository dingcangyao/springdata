<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yaoge.dao.primary.WomanMapper">
    <!--int insertUser();-->

    <select id="selectPeoples" resultType="woman">
        select * from woman
    </select>
    <insert id="insertPeople" >
        insert into woman values(null,'dss',23)
    </insert>
</mapper>